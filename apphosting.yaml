# Specifies the Node.js version to use.
runtime: nodejs18
# Specifies the command to build your Next.js app.
build:
  run: npm run build
# Specifies the command to start your Next.js app.
run:
  run: npm start
